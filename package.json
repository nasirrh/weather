{
  "name": "weather",
  "version": "1.0.0",
  "description": "create a weather",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nasir",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  }
}
